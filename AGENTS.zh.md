# Autryn Agent 工作指南

本文档适用于 Autryn 仓库根目录及其子目录，约束 Coding Agent 的思考方式、工作流程和安全边界。项目事实以源码、测试和配置为准；项目架构、运行机制与工程知识从 [.wiki/index.md](.wiki/index.md) 进入。

> Autryn 运行时自动加载项目根目录下的 `AGENTS.md`。本文件是中文候选稿，除非调用方显式读取，否则不会自动生效。

## 1. 第一性原理

- 先明确目标、约束、输入、输出和验收证据，再选择实现方式。
- 从系统必须满足的结果和不变量出发，把问题拆解为可验证事实、因果关系和必要条件，不从文件名、表象或既有做法直接推导结论。
- 区分已确认事实、合理推断和未知信息；能通过源码、测试、配置或运行结果确认的内容，不依赖猜测。
- 优先解决根因。不得通过吞掉异常、放宽类型、删除断言、绕过审批或增加特殊分支掩盖问题。
- 选择满足目标的最小充分改动。避免未被需求或证据支持的功能、抽象、配置项和扩展点。
- 重要决策应能说明问题、依据、取舍、风险和验证方式。

## 2. 信息获取

1. 检查工作区状态，识别用户已有修改和当前任务边界。
2. 从 [Wiki 入口](.wiki/index.md) 判断任务所属模块、Workflow、Interface 或工程领域。
3. 阅读目标 Wiki 页面，并从其 `sources` 定位最小事实范围。
4. 阅读目标实现、直接依赖、对应测试和相关配置；只有调用关系或风险需要时才继续扩展。

README 面向使用者，Agent 指南约束工作方式，Wiki 组织稳定知识，Git 保存历史。文档与实现发生冲突时，先核对当前行为和预期契约，再直接修正失效的一侧。

## 3. 事实与验证

- 区分静态阅读、自动检查、实际运行和外部环境观察；一种证据不能替代另一种证据。
- 未执行的命令、未覆盖的平台和未观察的运行结果不得描述为已通过。
- 项目命令以 `package.json`、脚本和配置中的当前定义为准，不凭记忆假定命令存在。
- `dist/`、`node_modules/`、缓存、临时文件和本地运行状态不是源码事实来源。
- 验证范围与改动风险相匹配；局部检查不能证明跨模块、跨平台或发布级结论。

## 4. 范围与改动原则

- 只处理当前任务，保留用户已有改动，不混入顺手重构、批量改名或无关格式化。
- 回答、审查和诊断任务默认只读；只有用户要求修改或任务明确包含实现时，才改动相应文件。
- 文档任务不授权修改业务代码、配置、依赖或外部系统；实现任务也只授权达成目标所需的影响面。
- 修改前确认职责归属、依赖方向、Public API、持久化、安全和跨平台影响。
- 优先复用现有领域抽象，避免在 CLI、TUI、测试或 Provider 中复制业务逻辑。
- 代码以清晰、正确、可维护为目标。简洁不等于压缩表达；函数与文件按单一职责、内聚性和可独立验证程度决定拆分或合并。
- 保持目标文件的现有风格。文件和目录使用 kebab-case，类型使用 PascalCase，函数与变量使用 camelCase，类型导入使用 `import type`。
- 使用 Agent、Runtime、Execution、Provider、Session、Turn、Tool、Skill、Middleware、Delegation、CLI、TUI 等通用英文术语。
- 路径、Shell、用户 Home 和二进制逻辑同时考虑 Windows、macOS 与 Linux。

## 5. 受保护边界

以下内容属于稳定或安全边界，修改前必须阅读对应 Wiki 页面、源码和测试：

- 模块职责与依赖方向：[系统架构](.wiki/architecture/system.md)。
- Agent、Tool 与 Delegation：[Agent 执行流程](.wiki/workflows/agent-execution.md)、[Tool 执行流程](.wiki/workflows/tool-execution.md)、[Delegation](.wiki/workflows/delegation.md)。
- Session 与 Model：[Session 生命周期](.wiki/workflows/session-lifecycle.md)、[Model 选择](.wiki/workflows/model-selection.md)。
- Package Export：[Public API](.wiki/interfaces/public-api.md)。
- Tool Approval、路径和敏感数据：[安全与权限](.wiki/engineering/security-and-permissions.md)。

不得绕过 Zod 校验、Tool Approval、Session lease、revision、路径安全或取消传播。不得泄露 API Key、Token、用户对话或其他敏感数据。未经任务明确要求，不改变公开类型、Command、Tool wire name、Session Schema 或持久化语义。

## 6. 实现要求

- 输入在系统边界校验；可预期失败使用既有结构化错误，不把普通输入错误抛出到顶层流程。
- 异步、流式、取消、timeout、并发和资源限制必须检查成功与失败路径。
- 有副作用的 Tool 需要评估审批、持久授权、路径范围和取消行为。
- 修改公共入口时同步检查源码 Export、`package.json#exports`、构建入口、Package Export 冒烟测试和使用示例。
- 修改 Tool 展示时同步检查 ANSI 与 Ink 两条 Renderer，并优先维护共享摘要逻辑。
- 不直接编辑 `dist/`、`node_modules/`、缓存或其他生成产物。

详细项目关系从 [Wiki](.wiki/index.md) 按需加载；具体格式以 TypeScript、ESLint、Prettier、目标文件和相邻测试的当前约束为准。

## 7. 测试与验证

- Bug 修复增加能够证明根因已消除的回归测试。
- 优先测试公共行为、状态转换、Schema、路径、持久化、权限、取消、并发和错误边界。
- Provider 测试不调用真实外部 API；文件系统测试使用独立临时目录并清理。
- 不要求为无风险透传机械增加测试，但不能用人工判断替代高风险行为的自动验证。
- 先运行最相关检查，完成前运行与风险相称的完整质量门。

主要质量门是 `bun run check`。Public API 或构建入口变化时，再运行 `bun run build:js` 和 Package Export 冒烟测试；平台二进制变化时运行相应构建并说明未覆盖平台。完整策略见[测试](.wiki/engineering/testing.md)和[构建与发布](.wiki/engineering/build-and-release.md)。

## 8. 文档维护

- 面向目标读者和实际问题组织内容，只保留理解、使用或维护所必需的信息。
- 文档只陈述当前成立的事实、关系、适用范围和操作方式；事实变化时直接重写权威位置，由 Git 保存历史。
- 新知识应融入原有结构和上下文，同时删除失效或重复内容，不通过文末补充、修订说明或新旧对比维持多个版本。
- 先给结论，再给必要依据；一段围绕一个中心事实，使用客观、平实的语言和稳定术语。
- 正面说明系统具备的能力、成立的关系和必须遵守的边界。限制条件应写清适用范围与影响，但不以纠错过程组织正文。
- 每项确定陈述都应由当前事实来源直接支持；推断、建议和待确认内容不能写成既定事实。
- 一个知识主题只选择一个主要文档位置，其他位置保留必要摘要和链接。稳定知识进入 Wiki，精确且易变的细节留在源码、测试、配置或 CLI Help。
- 注释解释意图、原因、不变量和非显然约束，不逐行复述代码，也不记录修改过程。
- 示例与当前公开接口一致，不使用真实密钥、个人路径或无法跨平台理解的默认值。
- 修改公共行为、模块职责、跨模块流程、持久化语义或安全边界时，按 [.wiki/SCHEMA.md](.wiki/SCHEMA.md) 执行 Refresh 与 Lint。

文档的内容归属以[文档职责](.wiki/engineering/documentation-ownership.md)为准。Git 承担时间线；Wiki 不保存 Raw、Log、Archive、Roadmap 或任务总结。

## 9. 工作流程

### 开始前

1. 明确任务与验收标准。
2. 检查工作区和相关 Wiki。
3. 阅读事实来源并形成可验证方案。

### 实现时

1. 保持改动集中、可逆、可审查。
2. 同步必要的类型、Schema、测试、Export 和文档。
3. 每完成一个关键行为就执行最小相关验证。

### 完成前

1. 运行质量检查并记录结果。
2. 检查差异、未跟踪文件和生成产物。
3. 确认用户已有修改未被覆盖，文档与当前实现一致。
4. 说明未执行的检查、剩余风险和需要用户决定的事项。

## 10. 权限与不可逆操作

- 未经用户明确授权，不执行发布、推送、创建 Tag、永久删除 Session 或其他不可逆外部操作。
- 删除、覆盖、迁移文件前确认目标和恢复方式；范围不清时停止并请求确认。
- 不使用破坏性命令清理工作区，不擅自回退、覆盖或提交用户改动。

## 11. 交付

交付说明聚焦结果：修改内容、关键行为、验证结果、未覆盖范围和剩余风险。不要只报告“已完成”，也不要把内部工具调用过程当成交付结果。
